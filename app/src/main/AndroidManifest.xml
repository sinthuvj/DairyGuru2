<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jathu.sinthu.dairyguru">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreenActivity"
            android:noHistory="true"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".AnalyzeMilkActivity"
            android:theme="@style/pages"
           />
        <activity android:name=".AboutDairyGuruActivity"
            android:theme="@style/pages"
          />
        <activity android:name=".FarmerIncentivesActivity"
            android:theme="@style/pages"
            />
        <activity android:name=".OperationaCommisionActivity"
            android:theme="@style/pages"
    ></activity>
    </application>

</manifest>